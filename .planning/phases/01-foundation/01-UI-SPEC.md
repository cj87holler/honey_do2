---
phase: 1
slug: foundation
status: draft
shadcn_initialized: false
preset: none
created: 2026-03-28
---

# Phase 1 — UI Design Contract

> Visual and interaction contract for Phase 1: Foundation.
> Generated by gsd-ui-researcher. Verified by gsd-ui-checker.

---

## Design System

| Property | Value | Source |
|----------|-------|--------|
| Tool | none | CLAUDE.md: "bee-themed app wants bespoke visuals, not stock shadcn defaults" |
| Preset | not applicable | — |
| Component library | none | CLAUDE.md: add shadcn selectively only if needed; Phase 1 has no complex dialogs or dropdowns |
| Icon library | Lucide React | Standard for Next.js + Tailwind projects; has `Crown` icon for Queen role badge (D-13) |
| Font | System font stack | Phase 5 owns personality fonts; Phase 1 stays functional-first |

**Note on icons:** D-13 requires a crown icon (Queen) and bee icon (Bee). Lucide has `Crown`. Lucide does not have a dedicated bee icon — use a Unicode bee emoji rendered in a small inline `<span>` as the Bee role indicator. This is intentionally minimal; Phase 5 will replace with custom iconography.

---

## Screens in Scope

Phase 1 delivers exactly five screen contexts. All copywriting and state coverage below maps to these screens.

| Screen | Route | Purpose |
|--------|-------|---------|
| Sign Up | `/signup` | Create account with email, password, display name |
| Log In | `/login` | Authenticate with email and password |
| Create Hive | `/hive/create` | Post-signup, single-field Hive naming (D-01, D-05) |
| Hive Dashboard | `/hive/[id]` | Landing after Hive creation; shows Hive name, member list with role badges (D-02, D-13) |
| Shared Header/Nav | — | Persistent across all `/hive/*` routes; contains logout (AUTH-03) |

---

## Spacing Scale

Standard 8-point scale. No exceptions for Phase 1.

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon-to-label gaps, role badge internal padding |
| sm | 8px | Input label to input gap, button icon gap |
| md | 16px | Default element spacing, form field gap |
| lg | 24px | Card padding, section spacing within a page |
| xl | 32px | Between major sections (e.g., form + footer link) |
| 2xl | 48px | Page-level vertical padding (top/bottom of main content area) |
| 3xl | 64px | Not used in Phase 1 |

Exceptions: none.

---

## Typography

System font stack throughout Phase 1. Phase 5 will introduce a custom font.

| Role | Size | Weight | Line Height | Usage |
|------|------|--------|-------------|-------|
| Body | 16px | 400 | 1.5 | Form labels, member list rows, paragraph copy |
| Label | 14px | 400 | 1.5 | Input helper text, role badge text, nav secondary text |
| Heading | 20px | 600 | 1.2 | Page headings (e.g., "Create your Hive", "Welcome to [Hive Name]") |
| Display | 28px | 600 | 1.2 | Hive name displayed prominently on the dashboard |

Source: template defaults. No upstream font specification — Phase 5 owns typography personality.

---

## Color

Color tokens from RESEARCH.md Tailwind CSS v4 example (`@theme` block).

| Role | Value | Usage | Source |
|------|-------|-------|--------|
| Dominant (60%) | `#ffffff` | Page backgrounds, form card backgrounds | Default; Phase 5 adds honeycomb patterns |
| Secondary (30%) | `#fafaf9` (`stone-50`) | Nav bar background, member list row backgrounds, input backgrounds | Warm near-white to complement amber accent |
| Accent (10%) | `#f59e0b` (`honey`) | Primary CTA buttons, active nav indicator, role badge border | RESEARCH.md `--color-honey: #f59e0b` |
| Text primary | `#1c1917` (`bee`) | All body text, headings | RESEARCH.md `--color-bee: #1c1917` |
| Text secondary | `#78716c` (`stone-500`) | Helper text, placeholder text, secondary labels | Default; complements warm stone palette |
| Queen badge | `#92400e` (`queen`) | Queen role badge background | RESEARCH.md `--color-queen: #92400e` |
| Accent light | `#fde68a` (`honey-light`) | Hover state on accent buttons, form focus ring | RESEARCH.md `--color-honey-light: #fde68a` |
| Destructive | `#dc2626` (`red-600`) | Destructive actions (none in Phase 1, declared for completeness) | Standard red |

**Accent reserved for:**
- Primary CTA button fill ("Create Hive", "Sign Up", "Log In")
- Active/current nav indicator
- Role badge border for Queen badges

Never apply accent to body text, secondary buttons, or decorative backgrounds.

**Tailwind CSS v4 `@theme` block:**
```css
@import "tailwindcss";

@theme {
  --color-honey: #f59e0b;
  --color-honey-light: #fde68a;
  --color-queen: #92400e;
  --color-bee: #1c1917;
}
```

---

## Copywriting Contract

### Authentication Screens

| Element | Copy |
|---------|------|
| Sign Up page heading | "Create your account" |
| Sign Up display name label | "Your name" |
| Sign Up display name placeholder | "How your hivemates will see you" |
| Sign Up primary CTA | "Sign up" |
| Sign Up footer link | "Already have an account? Log in" |
| Log In page heading | "Welcome back" |
| Log In primary CTA | "Log in" |
| Log In footer link | "Don't have an account? Sign up" |
| Log Out nav item | "Log out" |

### Hive Creation Screen

| Element | Copy |
|---------|------|
| Page heading | "Create your Hive" |
| Page subheading | "Give your Hive a name. You can change it anytime." |
| Input label | "Hive name" |
| Input placeholder | "The Johnson Family" |
| Primary CTA | "Create Hive" |

### Hive Dashboard

| Element | Copy |
|---------|------|
| Hive name display | `[Hive Name]` (dynamic, 28px Display) |
| Dashboard empty state heading | "Your Hive is ready." |
| Dashboard empty state body | "Start by inviting your housemates. Once everyone is in, you can assign tasks and see who earns the most honeys." |
| Member section heading | "Members" |
| Member list empty state (secondary, below Queen's own row) | "No other members yet." |
| Role label — Queen | "Queen" (with Crown icon) |
| Role label — Bee | "Bee" (with bee emoji) |

**Dashboard empty state layout note:** After Hive creation the dashboard shows: Hive name (Display), the empty state copy block, then the Members section showing the Queen's own row. This is the only guaranteed non-empty area in Phase 1.

### Validation and Error States

| Scenario | Copy |
|----------|------|
| Email already taken (sign up) | "That email is already registered. Try logging in instead." |
| Invalid email format | "Enter a valid email address." |
| Password too short | "Password must be at least 8 characters." |
| Password required | "Enter a password." |
| Email required | "Enter your email." |
| Display name required | "Enter your name." |
| Display name too long (>100 chars) | "Name must be 100 characters or fewer." |
| Wrong email or password (log in) | "Incorrect email or password." |
| Hive name empty | "Give your Hive a name." |
| Hive name too long (>100 chars) | "Hive name must be 100 characters or fewer." |
| Generic server error | "Something went wrong. Please try again." |

### Destructive Actions

No destructive actions in Phase 1 scope (no deletes). Declared here for completeness.

| Action | Confirmation approach |
|--------|----------------------|
| none | — |

---

## Interaction Contracts

### Form Submission Pattern

All forms in Phase 1 use the same interaction pattern:

1. Client-side validation runs on submit (react-hook-form + Zod)
2. Inline error messages appear below each invalid field immediately on submit
3. CTA button shows a disabled loading state with the button text replaced by "Loading..." while the Server Action is in flight
4. On server error, an inline error banner appears above the form (not a toast)
5. On success, the browser navigates to the next screen (no success toast needed — navigation is the confirmation)

### Role Badge Display (D-13)

Roles displayed as subtle inline badges next to user name in the member list:

- **Queen:** Small badge with `#92400e` (queen) background, white text, "Queen", preceded by a `Crown` icon (Lucide, 14px)
- **Bee:** Small badge with `stone-100` background, `#1c1917` text, "Bee", preceded by bee emoji (14px equivalent)
- Badge height: 20px. Internal padding: 4px horizontal, 2px vertical. Border-radius: 4px.

### Hive Name Rename (D-06)

Inline edit — no confirmation gate, no settings page. The Hive name on the dashboard is directly editable. Click/tap on the name enters an inline text input; pressing Enter or blurring the field saves immediately via Server Action. No undo, no modal.

---

## Registry Safety

No third-party component registries in use. shadcn not initialized.

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable — not initialized |
| third-party | none | not applicable |

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending

---

## Source Traceability

| Design Decision | Source |
|-----------------|--------|
| No shadcn / no component library | CLAUDE.md: "bee-themed app wants bespoke visuals" |
| Honey amber `#f59e0b` as accent | RESEARCH.md `@theme` block, Tailwind CSS v4 example |
| Bee dark `#1c1917` as text primary | RESEARCH.md `--color-bee` |
| Queen brown `#92400e` for Queen badge | RESEARCH.md `--color-queen` |
| Two roles only: Queen / Bee | CONTEXT.md D-08 |
| Crown icon for Queen, bee emoji for Bee | CONTEXT.md D-13 |
| Single-field Hive creation (no wizard) | CONTEXT.md D-05 |
| Inline Hive rename, no confirmation gate | CONTEXT.md D-06 |
| Post-signup lands on "Create Hive" | CONTEXT.md D-01 |
| After Hive creation lands on dashboard | CONTEXT.md D-02 |
| System font stack | Default — Phase 5 owns personality font (THEME-01) |
| 8-point spacing scale | Template default — no exceptions needed for Phase 1 screens |
| 16px body / 400 weight / 1.5 line-height | Template default |
| Loading state: disabled button with "Loading..." text | Default — picked one; no spinner needed for Phase 1 |
| Dashboard empty state copy | Default — explains Hive is ready, prompts toward invites |
| Display name field copy | Default — "Your name" label, hivemates framing in placeholder |
