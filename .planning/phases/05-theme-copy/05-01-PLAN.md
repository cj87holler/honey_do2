---
phase: 05-theme-copy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/components/ui/honeycomb-pattern.tsx
  - src/components/layout/header.tsx
  - src/components/ui/button.tsx
autonomous: true
requirements:
  - THEME-01
must_haves:
  truths:
    - "Every page has a subtle honeycomb wallpaper pattern visible behind content"
    - "App header has amber/gold background with bee emoji, title, and tagline"
    - "Header logout button has readable contrast on amber background"
    - "All interactive elements have 44px minimum touch targets on mobile (375px)"
    - "Honeycomb pattern degrades gracefully on small screens"
  artifacts:
    - path: "src/app/globals.css"
      provides: "Honeycomb CSS variables, SVG pattern, completion flash keyframe"
      contains: "--color-honeycomb-pattern"
    - path: "src/components/ui/honeycomb-pattern.tsx"
      provides: "Reusable honeycomb background wrapper component"
      exports: ["HoneycombPattern"]
    - path: "src/components/layout/header.tsx"
      provides: "Bee-themed amber header with tagline"
      contains: "bg-honey"
    - path: "src/components/ui/button.tsx"
      provides: "Mobile-responsive button with 44px touch targets"
      contains: "h-11"
  key_links:
    - from: "src/app/globals.css"
      to: "src/components/ui/honeycomb-pattern.tsx"
      via: "CSS custom properties consumed by pattern component"
      pattern: "--color-honeycomb"
    - from: "src/components/layout/header.tsx"
      to: "src/app/globals.css"
      via: "Tailwind custom colors"
      pattern: "bg-honey|text-queen"
---

<objective>
Establish the bee-theme visual foundation: honeycomb CSS pattern, amber header redesign, mobile touch target fixes, and completion flash keyframe.

Purpose: Every subsequent component change in Plan 02 builds on these CSS variables, pattern component, and header. This plan makes the app "feel like a hive" at the page level before component-level theming.
Output: globals.css with honeycomb vars + keyframe, HoneycombPattern wrapper, redesigned header, mobile-ready button.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-theme-copy/05-CONTEXT.md
@.planning/phases/05-theme-copy/05-UI-SPEC.md

@src/app/globals.css
@src/components/layout/header.tsx
@src/components/ui/button.tsx
@src/lib/utils.ts

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/components/ui/button.tsx:
```typescript
interface ButtonProps extends ButtonHTMLAttributes<HTMLButtonElement> {
  variant?: "primary" | "secondary" | "ghost"
  size?: "sm" | "md"
}
```

From src/lib/utils.ts:
```typescript
export function cn(...inputs: ClassValue[]): string
```

From src/app/globals.css (current):
```css
@import "tailwindcss";
@theme {
  --color-honey: #f59e0b;
  --color-honey-light: #fde68a;
  --color-queen: #92400e;
  --color-bee: #1c1917;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CSS foundation — honeycomb variables, SVG pattern, keyframes, and HoneycombPattern component</name>
  <files>src/app/globals.css, src/components/ui/honeycomb-pattern.tsx</files>
  <read_first>src/app/globals.css, src/components/ui/button.tsx, .planning/phases/05-theme-copy/05-UI-SPEC.md</read_first>
  <action>
**globals.css** — Extend the existing `@theme` block with these new CSS custom properties (per UI-SPEC Color section and Honeycomb Pattern Specification):

```css
@import "tailwindcss";

@theme {
  --color-honey: #f59e0b;
  --color-honey-light: #fde68a;
  --color-queen: #92400e;
  --color-bee: #1c1917;
  --color-honeycomb-pattern: rgba(245, 158, 11, 0.08);
  --color-honeycomb-stroke: rgba(245, 158, 11, 0.18);
  --color-header-bg: #f59e0b;
  --color-header-text: #78350f;
}
```

Below the `@theme` block, add:

1. A `.honeycomb-bg` utility class that applies a repeating hexagonal SVG pattern as `background-image` using an inline SVG data URI. The hexagon grid should tile at `56px x 100px`. Stroke color uses `rgba(245, 158, 11, 0.18)`, fill uses `rgba(245, 158, 11, 0.08)`. The SVG should draw a single hexagon that tiles seamlessly.

2. A media query `@media (max-width: 375px)` that reduces `.honeycomb-bg` pattern opacity to 4% (change fill to `rgba(245, 158, 11, 0.04)` and stroke to `rgba(245, 158, 11, 0.08)`).

3. A `@keyframes completion-flash` keyframe animation:
   - 0%: `background-color: transparent`
   - 50%: `background-color: rgba(245, 158, 11, 0.25)` (amber flash)
   - 100%: `background-color: transparent`

4. A `.animate-completion-flash` class that applies `animation: completion-flash 600ms ease-out`.

5. A `.animate-card-reveal` class with `animation: card-reveal 300ms ease-out forwards` and `opacity: 0` initial state. Define `@keyframes card-reveal` as: 0% `opacity: 0; transform: translateY(4px)`, 100% `opacity: 1; transform: translateY(0)`.

**HoneycombPattern component** — Create `src/components/ui/honeycomb-pattern.tsx` as a server component:

```typescript
import { cn } from "@/lib/utils"

interface HoneycombPatternProps {
  children: React.ReactNode
  className?: string
  intensity?: "subtle" | "medium"
}

export function HoneycombPattern({ children, className, intensity = "subtle" }: HoneycombPatternProps) {
  return (
    <div className={cn("honeycomb-bg", intensity === "medium" && "honeycomb-bg-medium", className)}>
      {children}
    </div>
  )
}
```

Add a `.honeycomb-bg-medium` variant in globals.css that uses 12% fill opacity (`rgba(245, 158, 11, 0.12)`) for section-level emphasis per UI-SPEC.
  </action>
  <verify>
    <automated>cd /Users/cj.holler/Desktop/honey_do2 && grep -c "honeycomb-pattern\|honeycomb-stroke\|header-bg\|header-text\|completion-flash\|card-reveal\|honeycomb-bg" src/app/globals.css && grep -c "HoneycombPattern" src/components/ui/honeycomb-pattern.tsx</automated>
  </verify>
  <acceptance_criteria>
    - globals.css contains `--color-honeycomb-pattern: rgba(245, 158, 11, 0.08)`
    - globals.css contains `--color-honeycomb-stroke: rgba(245, 158, 11, 0.18)`
    - globals.css contains `--color-header-bg: #f59e0b`
    - globals.css contains `--color-header-text: #78350f`
    - globals.css contains `@keyframes completion-flash`
    - globals.css contains `.animate-completion-flash`
    - globals.css contains `@keyframes card-reveal`
    - globals.css contains `.animate-card-reveal`
    - globals.css contains `.honeycomb-bg` with SVG data URI background-image
    - globals.css contains `.honeycomb-bg-medium` with 12% opacity variant
    - globals.css contains `@media (max-width: 375px)` block reducing honeycomb opacity
    - `src/components/ui/honeycomb-pattern.tsx` exports `HoneycombPattern`
    - HoneycombPattern accepts `children`, `className`, and `intensity` props
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>All CSS variables, keyframes, pattern classes, and HoneycombPattern component exist. TypeScript compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Header redesign + Button mobile touch targets</name>
  <files>src/components/layout/header.tsx, src/components/ui/button.tsx</files>
  <read_first>src/components/layout/header.tsx, src/components/ui/button.tsx, src/app/globals.css, .planning/phases/05-theme-copy/05-UI-SPEC.md</read_first>
  <action>
**header.tsx** — Redesign per D-04 and UI-SPEC "App Header Redesign" section. Replace the current neutral header with:

```tsx
"use client"

import { authClient } from "@/lib/auth-client"
import { Button } from "@/components/ui/button"
import { useRouter } from "next/navigation"

export function Header() {
  const router = useRouter()

  const handleLogout = async () => {
    await authClient.signOut()
    router.push("/login")
  }

  return (
    <header className="bg-honey">
      <div className="mx-auto flex h-14 max-w-3xl items-center justify-between px-4">
        <div>
          <span className="text-xl font-bold text-queen">
            🐝 Honey Do
          </span>
          <p className="hidden sm:block text-xs font-normal text-amber-800 opacity-80">
            Your Hive. Your Rules. Your Honey.
          </p>
        </div>
        <Button
          variant="ghost"
          size="sm"
          onClick={handleLogout}
          className="text-amber-900 hover:bg-amber-600/20"
        >
          Log out
        </Button>
      </div>
    </header>
  )
}
```

Key changes from current:
- Background: `bg-stone-50` -> `bg-honey` (amber-500 / #f59e0b per D-04)
- Border: remove `border-b border-stone-200`
- Title: `text-base font-semibold text-bee` -> `text-xl font-bold text-queen` with 🐝 emoji prefix
- Add tagline `<p>` below title: "Your Hive. Your Rules. Your Honey." — hidden on mobile via `hidden sm:block` per UI-SPEC Mobile Contract
- Logout button: override ghost hover to `text-amber-900 hover:bg-amber-600/20` for contrast on amber bg

**button.tsx** — Add mobile touch target responsiveness per UI-SPEC Mobile Contract. The `sm` size currently uses `h-8 px-3 text-sm` (32px height). On mobile (below `sm:` breakpoint = 640px), buttons must be 44px tall minimum.

Update the size classes:
- `size === "sm"`: change from `"h-8 px-3 text-sm"` to `"h-11 sm:h-8 px-3 text-sm"` — 44px on mobile, 32px on desktop
- `size === "md"`: keep `"h-10 px-4 text-base"` — already 40px, close enough (leave as-is or bump to `"h-11 sm:h-10 px-4 text-base"` for strict 44px compliance)

Also change `transition-colors` to `transition-all duration-150` per UI-SPEC Micro-interactions "Button Hover" section to support scale transforms.
  </action>
  <verify>
    <automated>cd /Users/cj.holler/Desktop/honey_do2 && grep "bg-honey" src/components/layout/header.tsx && grep "text-queen" src/components/layout/header.tsx && grep "Your Hive" src/components/layout/header.tsx && grep "h-11" src/components/ui/button.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - header.tsx contains `className="bg-honey"` on the header element (not `bg-stone-50`)
    - header.tsx does NOT contain `border-b border-stone-200`
    - header.tsx contains `text-xl font-bold text-queen`
    - header.tsx contains the emoji 🐝 before "Honey Do"
    - header.tsx contains the string "Your Hive. Your Rules. Your Honey."
    - header.tsx contains `hidden sm:block` on the tagline paragraph
    - header.tsx contains `text-amber-900 hover:bg-amber-600/20` on the logout button
    - button.tsx contains `h-11` for mobile touch targets in the sm size
    - button.tsx contains `transition-all duration-150` instead of `transition-colors`
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Header displays amber background with bee emoji, bold title, tagline (hidden on mobile), contrast-adjusted logout. Buttons have 44px touch targets on mobile. TypeScript compiles.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes (no type errors)
- `grep -c "honeycomb-pattern\|header-bg\|completion-flash" src/app/globals.css` returns 3+
- `grep "bg-honey" src/components/layout/header.tsx` shows amber header
- `grep "h-11" src/components/ui/button.tsx` shows mobile touch targets
- Visual: header is amber with bee emoji and tagline, pages show subtle hex pattern
</verification>

<success_criteria>
- globals.css has 4 new color vars, honeycomb SVG pattern class, completion flash keyframe, card reveal keyframe
- HoneycombPattern server component exists and exports correctly
- Header is amber with bee emoji, title in queen color, tagline hidden on mobile
- Button sm size is 44px on mobile, 32px on desktop
- All transitions upgraded to transition-all duration-150
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-theme-copy/05-01-SUMMARY.md`
</output>
