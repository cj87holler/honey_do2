---
phase: 05-theme-copy
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/components/hive/dashboard-greeting.tsx
  - src/components/hive/hive-dashboard.tsx
  - src/components/tasks/task-card.tsx
  - src/components/tasks/honeycomb.tsx
  - src/components/tasks/all-tasks.tsx
  - src/components/hive/leaderboard.tsx
  - src/app/layout.tsx
autonomous: false
requirements:
  - THEME-01
  - THEME-02
must_haves:
  truths:
    - "Dashboard shows a personalized greeting with user's name that changes based on active task count"
    - "Each of the 4 task-load states (0, 1-3, 4-7, 8+) has at least 8 copy variants"
    - "Task status badges use bee-themed amber colors instead of blue/green"
    - "Completing a task triggers a 600ms amber flash on the task card"
    - "Task cards in Honeycomb fade in with staggered animation on mount"
    - "Leaderboard rank-1 row has a left amber border for emphasis"
    - "Empty states use bee-themed copy with puns"
    - "Honeycomb pattern is visible on the page background"
  artifacts:
    - path: "src/components/hive/dashboard-greeting.tsx"
      provides: "Dynamic copy engine with 32 variants across 4 task-load states"
      exports: ["DashboardGreeting"]
      min_lines: 60
    - path: "src/components/hive/hive-dashboard.tsx"
      provides: "Dashboard with greeting inserted above task sections"
      contains: "DashboardGreeting"
    - path: "src/components/tasks/task-card.tsx"
      provides: "Bee-themed status badges and completion flash"
      contains: "amber-100"
    - path: "src/components/tasks/honeycomb.tsx"
      provides: "Card reveal animation and updated empty state"
      contains: "card-reveal"
    - path: "src/components/hive/leaderboard.tsx"
      provides: "Rank-1 amber left border"
      contains: "border-l-4 border-amber-400"
  key_links:
    - from: "src/components/hive/dashboard-greeting.tsx"
      to: "src/components/hive/hive-dashboard.tsx"
      via: "Import and render with name + activeTaskCount props"
      pattern: "DashboardGreeting"
    - from: "src/components/tasks/task-card.tsx"
      to: "src/app/globals.css"
      via: "animate-completion-flash CSS class"
      pattern: "animate-completion-flash"
    - from: "src/app/layout.tsx"
      to: "src/components/ui/honeycomb-pattern.tsx"
      via: "Wraps page content with honeycomb background"
      pattern: "HoneycombPattern"
---

<objective>
Build the dynamic copy engine, apply bee theme to all components, add micro-interactions, wire honeycomb background into the layout.

Purpose: This plan delivers the user-facing personality of the app — the greeting engine (THEME-02), bee-themed badges, completion flash, card animations, pun copy, and the page-level honeycomb pattern. After this plan, the app looks and feels like a bee hive.
Output: DashboardGreeting component with 32 copy variants, themed task-card/honeycomb/leaderboard/all-tasks, honeycomb background on layout.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-theme-copy/05-CONTEXT.md
@.planning/phases/05-theme-copy/05-UI-SPEC.md
@.planning/phases/05-theme-copy/05-01-SUMMARY.md

@src/components/hive/hive-dashboard.tsx
@src/components/tasks/task-card.tsx
@src/components/tasks/honeycomb.tsx
@src/components/tasks/all-tasks.tsx
@src/components/hive/leaderboard.tsx
@src/app/layout.tsx
@src/app/globals.css

<interfaces>
<!-- From hive-dashboard.tsx -->
```typescript
interface HiveDashboardProps {
  hive: { id: string; name: string }
  members: Member[]
  currentUserId: string
  tasks: Task[]
}
```

<!-- From task-card.tsx -->
```typescript
interface TaskCardProps {
  task: {
    id: string; text: string; honeyValue: number;
    status: "open" | "in_progress" | "done";
    assigneeId: string; assigneeName: string; completedAt: Date | null;
  }
  currentMemberId: string | undefined
  isQueen: boolean
  hiveId: string
}
```

<!-- From honeycomb.tsx -->
```typescript
interface HoneycombProps {
  tasks: HoneycombTask[]
  currentMemberId: string | undefined
  isQueen: boolean
  hiveId: string
}
```

<!-- From Plan 01 output -->
```typescript
// src/components/ui/honeycomb-pattern.tsx
export function HoneycombPattern({ children, className, intensity }: HoneycombPatternProps): JSX.Element
// src/app/globals.css adds: .honeycomb-bg, .animate-completion-flash, .animate-card-reveal
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: DashboardGreeting copy engine + wire into dashboard + honeycomb bg on layout</name>
  <files>src/components/hive/dashboard-greeting.tsx, src/components/hive/hive-dashboard.tsx, src/app/layout.tsx</files>
  <read_first>src/components/hive/hive-dashboard.tsx, src/app/layout.tsx, src/components/ui/honeycomb-pattern.tsx, .planning/phases/05-theme-copy/05-UI-SPEC.md</read_first>
  <action>
**dashboard-greeting.tsx** — Create as a client component (`"use client"`) at `src/components/hive/dashboard-greeting.tsx`.

Props interface:
```typescript
interface DashboardGreetingProps {
  name: string
  activeTaskCount: number
}
```

Copy registry — define a constant object with 4 keys: `zero`, `light`, `moderate`, `heavy`. Each key maps to an array of 8 template strings. Use `{name}` and `{count}` as placeholders.

**Zero tasks (0 active) — 8 variants:**
1. `"Hey {name}, your Honeycomb is empty. Go play golf — you've earned it! 🏌️"`
2. `"Hey {name}, nothing in your Honeycomb. The hive is at peace. 🌻"`
3. `"Hey {name}, all done! The bees are impressed. Seriously. 🐝"`
4. `"Hey {name}, zero tasks. Absolute legend. The Queen approves. 👑"`
5. `"Hey {name}, your Honeycomb is sparkling clean. Take the afternoon off. ✨"`
6. `"Hey {name}, nothing to buzz about today. Rest those wings. 🍯"`
7. `"Hey {name}, task-free zone! You're the worker bee everyone aspires to be. 🏆"`
8. `"Hey {name}, empty Honeycomb energy. Rare. Powerful. Beautiful. 🌟"`

**Light load (1-3 active) — 8 variants:**
1. `"Hey {name}, just a few honey drops to collect. You've got this. 🍯"`
2. `"Hey {name}, {count} little tasks. A warm-up for a champion. 💪"`
3. `"Hey {name}, {count} tasks on your Honeycomb. Light work for a busy bee. 🐝"`
4. `"Hey {name}, {count} tasks buzzing your way. Knock 'em out! ⚡"`
5. `"Hey {name}, a light load today. Perfect time to be speedy. 🚀"`
6. `"Hey {name}, {count} tasks. The hive believes in you. So do we. 🌼"`
7. `"Hey {name}, just {count} things between you and sweet freedom. 🍋"`
8. `"Hey {name}, easy day! {count} tasks and then you're free as a bee. 🌈"`

**Moderate load (4-7 active) — 8 variants:**
1. `"Hey {name}, {count} tasks buzzing! Time to put on your work stripes. 🐝"`
2. `"Hey {name}, the Honeycomb is filling up. Let's get to it! 🏃"`
3. `"Hey {name}, {count} tasks in the hive. Full bee mode activated. ⚡"`
4. `"Hey {name}, {count} honeys waiting to be earned. Buzz buzz! 🍯"`
5. `"Hey {name}, mid-hive hustle mode. {count} tasks, let's gooo. 🔥"`
6. `"Hey {name}, {count} tasks on deck. This is what being a bee is about. 💛"`
7. `"Hey {name}, solid task load! {count} things between you and honey glory. 🏆"`
8. `"Hey {name}, {count} tasks. The Queen has high expectations. Don't disappoint. 👑"`

**Heavy load (8+ active) — 8 variants:**
1. `"Hey {name}, whoa! {count} tasks buzzing — your Honeycomb is PACKED! 🚨"`
2. `"Hey {name}, {count} tasks?! You're basically the whole hive. Let's go! 💥"`
3. `"Hey {name}, {count} tasks and counting. Hero mode: engaged. 🦸"`
4. `"Hey {name}, buzz buzz BUZZ! {count} tasks need your wings. Fly! 🐝🐝🐝"`
5. `"Hey {name}, the Honeycomb overflows! {count} tasks await. You can do this. 💪"`
6. `"Hey {name}, {count} tasks. Legend. Absolute legend. The bees sing your name. 🎶"`
7. `"Hey {name}, full hive alert! {count} tasks. Sting 'em one by one. 🎯"`
8. `"Hey {name}, {count} tasks! The Queen is watching. Don't keep her waiting. 👑🔥"`

State selection logic:
```typescript
function getLoadState(count: number): "zero" | "light" | "moderate" | "heavy" {
  if (count === 0) return "zero"
  if (count <= 3) return "light"
  if (count <= 7) return "moderate"
  return "heavy"
}
```

Use `useState` with a lazy initializer to pick a random variant index once on mount:
```typescript
const [variantIndex] = useState(() => Math.floor(Math.random() * 8))
```

Then select the message: `COPY_REGISTRY[getLoadState(activeTaskCount)][variantIndex]`, replacing `{name}` with `name` and `{count}` with `String(activeTaskCount)`.

Render in a banner container:
```tsx
<div className="rounded-xl bg-amber-50 border border-amber-200 px-4 py-3 sm:px-6 sm:py-4">
  <p className="text-xl sm:text-2xl font-bold text-bee">{message}</p>
</div>
```

Mobile responsive per UI-SPEC: `px-4 py-3` on mobile, `px-6 py-4` on desktop; Display text (24px) on desktop, Heading (20px) on mobile. Use `text-xl sm:text-2xl`.

**hive-dashboard.tsx** — Add import for `DashboardGreeting` and insert it as the first child inside the `<div className="space-y-8">`, BEFORE `InlineRename`. The dashboard needs the user's name — add `currentUserName: string` to `HiveDashboardProps`.

Compute activeTaskCount for the current member:
```typescript
const activeTaskCount = tasks.filter(
  (t) => t.assigneeId === currentMemberId && t.status !== "done"
).length
```

Insert:
```tsx
<DashboardGreeting name={currentUserName} activeTaskCount={activeTaskCount} />
```

The parent page that renders `HiveDashboard` must pass `currentUserName`. Read the page file that renders HiveDashboard (likely `src/app/(app)/hive/[id]/page.tsx` or similar) and add the prop. The user's name is available from the session — find where `currentUserId` is sourced and also grab `session.user.name`.

**layout.tsx** — Wrap the page content (the `{children}` in the root or app layout) with `HoneycombPattern` from `src/components/ui/honeycomb-pattern.tsx`:

```tsx
import { HoneycombPattern } from "@/components/ui/honeycomb-pattern"

// In the return, wrap the main content area:
<HoneycombPattern className="min-h-screen">
  {children}
</HoneycombPattern>
```

This applies the honeycomb wallpaper to every page. The `HoneycombPattern` component adds the `.honeycomb-bg` class from globals.css.
  </action>
  <verify>
    <automated>cd /Users/cj.holler/Desktop/honey_do2 && grep -c "Hey {name}" src/components/hive/dashboard-greeting.tsx && grep "DashboardGreeting" src/components/hive/hive-dashboard.tsx && grep "HoneycombPattern" src/app/layout.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/hive/dashboard-greeting.tsx` exists and contains `"use client"`
    - dashboard-greeting.tsx contains exactly 32 copy variant strings (8 per state x 4 states)
    - dashboard-greeting.tsx exports `DashboardGreeting`
    - dashboard-greeting.tsx contains `getLoadState` function with thresholds: 0, 1-3, 4-7, 8+
    - dashboard-greeting.tsx contains `useState(() => Math.floor(Math.random() * 8))`
    - dashboard-greeting.tsx contains `rounded-xl bg-amber-50 border border-amber-200`
    - dashboard-greeting.tsx contains `text-xl sm:text-2xl` for responsive display text
    - hive-dashboard.tsx imports and renders `DashboardGreeting` before `InlineRename`
    - hive-dashboard.tsx contains `currentUserName` in the props interface
    - hive-dashboard.tsx computes `activeTaskCount` from filtered tasks
    - layout.tsx imports `HoneycombPattern` and wraps children with it
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Dashboard shows a randomized, personalized greeting based on task load. Honeycomb wallpaper visible on every page via layout wrapper.</done>
</task>

<task type="auto">
  <name>Task 2: Bee-themed status badges, completion flash, card reveal, leaderboard rank-1, empty state copy</name>
  <files>src/components/tasks/task-card.tsx, src/components/tasks/honeycomb.tsx, src/components/tasks/all-tasks.tsx, src/components/hive/leaderboard.tsx</files>
  <read_first>src/components/tasks/task-card.tsx, src/components/tasks/honeycomb.tsx, src/components/tasks/all-tasks.tsx, src/components/hive/leaderboard.tsx, src/app/globals.css, .planning/phases/05-theme-copy/05-UI-SPEC.md</read_first>
  <action>
**task-card.tsx** — Two changes: status badge theming and completion flash micro-interaction.

1. **Status badges** — Replace the blue/green badge colors with bee-themed amber per UI-SPEC "Status Badge Theming":

   In Progress badge: change from `bg-blue-100 text-blue-700` to `bg-amber-100 text-amber-800`. Add bee emoji prefix:
   ```tsx
   {task.status === "in_progress" && (
     <span className="inline-flex items-center rounded-full bg-amber-100 px-2 py-0.5 text-xs font-medium text-amber-800">
       🐝 In Progress
     </span>
   )}
   ```

   Done badge: change from `bg-green-100 text-green-700` to `bg-amber-200 text-amber-900`. Add checkmark prefix:
   ```tsx
   {task.status === "done" && (
     <span className="inline-flex items-center rounded-full bg-amber-200 px-2 py-0.5 text-xs font-medium text-amber-900">
       ✅ Done
     </span>
   )}
   ```

2. **Completion flash** — Add `useEffect`, `useRef` imports. Add refs to detect when `task.status` becomes `"done"` and briefly apply the `animate-completion-flash` CSS class:

   ```typescript
   const cardRef = useRef<HTMLDivElement>(null)
   const prevStatusRef = useRef(task.status)

   useEffect(() => {
     if (prevStatusRef.current !== "done" && task.status === "done" && cardRef.current) {
       cardRef.current.classList.add("animate-completion-flash")
       const timer = setTimeout(() => {
         cardRef.current?.classList.remove("animate-completion-flash")
       }, 600)
       prevStatusRef.current = task.status
       return () => clearTimeout(timer)
     }
     prevStatusRef.current = task.status
   }, [task.status])
   ```

   Add `ref={cardRef}` to the outer `<div>` of the task card.

3. **Done button hover** — Add `hover:scale-105` to the Done button per UI-SPEC Micro-interactions:
   ```tsx
   <Button variant="primary" size="sm" onClick={handleDone} disabled={isPending}
     className="hover:scale-105">
     Done!
   </Button>
   ```

**honeycomb.tsx** — Two changes: card reveal animation and empty state copy.

1. **Card reveal animation** — Apply staggered `animate-card-reveal` to each TaskCard wrapper. Wrap each card in a div with the animation class and an inline `animationDelay`:
   ```tsx
   {activeTasks.map((task, index) => (
     <div
       key={task.id}
       className="animate-card-reveal"
       style={{ animationDelay: `${index * 50}ms` }}
     >
       <TaskCard task={task} currentMemberId={currentMemberId} isQueen={isQueen} hiveId={hiveId} />
     </div>
   ))}
   ```

   Do the same for the completed tasks list.

2. **Empty state copy** — Replace the current empty state text:
   - Current: `"No tasks in your Honeycomb yet. Time to relax!"`
   - New: Two lines per UI-SPEC Copywriting Contract:
   ```tsx
   <div className="text-center py-4">
     <p className="text-base font-semibold text-bee">Your Honeycomb is empty!</p>
     <p className="text-sm text-stone-500 mt-1">No tasks in your Honeycomb. The hive is quiet — enjoy it! 🐝</p>
   </div>
   ```

3. **Section heading** — Update `<h2>` from `text-lg font-semibold` to `text-xl font-bold` per UI-SPEC Typography (Heading role = 20px bold):
   ```tsx
   <h2 className="text-xl font-bold text-bee">Your Honeycomb</h2>
   ```

**all-tasks.tsx** — Two changes: empty state copy and heading.

1. **Empty state** — Replace current:
   - Current: `"No active tasks in the Hive yet. Assign one above!"`
   - New per UI-SPEC: `"No tasks assigned yet. Time to put your Bees to work! 🍯"`

2. **Heading** — Update to `text-xl font-bold` per Typography spec:
   ```tsx
   <h2 className="text-xl font-bold text-bee">All Tasks</h2>
   ```

3. **Card reveal animation** — Same staggered pattern as honeycomb.tsx for active task cards.

**leaderboard.tsx** — Two changes: rank-1 emphasis and heading.

1. **Rank-1 left border** — Add conditional `border-l-4 border-amber-400` to the rank-1 `<li>` per UI-SPEC Micro-interactions:
   ```tsx
   <li
     key={member.id}
     className={cn(
       "flex items-center justify-between py-2 px-4 bg-stone-50 rounded border-b border-stone-100 hover:bg-stone-100 transition-colors",
       member.rank === 1 && "border-l-4 border-amber-400"
     )}
   >
   ```

   Note: `cn` is already imported in leaderboard.tsx.

2. **Row hover** — Change `hover:bg-stone-100` to `hover:bg-amber-50` for bee-themed hover.

3. **Heading** — Update to `text-xl font-bold` per Typography spec (currently `text-xl font-semibold` — just change weight):
   ```tsx
   <h2 className="text-xl font-bold text-bee mb-3">Leaderboard</h2>
   ```
  </action>
  <verify>
    <automated>cd /Users/cj.holler/Desktop/honey_do2 && grep "bg-amber-100 text-amber-800" src/components/tasks/task-card.tsx && grep "animate-completion-flash" src/components/tasks/task-card.tsx && grep "animate-card-reveal" src/components/tasks/honeycomb.tsx && grep "border-l-4 border-amber-400" src/components/hive/leaderboard.tsx && grep "The hive is quiet" src/components/tasks/honeycomb.tsx && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - task-card.tsx In Progress badge uses `bg-amber-100 text-amber-800` (not blue)
    - task-card.tsx In Progress badge contains bee emoji prefix
    - task-card.tsx Done badge uses `bg-amber-200 text-amber-900` (not green)
    - task-card.tsx Done badge contains checkmark prefix
    - task-card.tsx contains `animate-completion-flash` class application
    - task-card.tsx contains `useRef` and `useEffect` for flash detection
    - task-card.tsx Done button contains `hover:scale-105`
    - honeycomb.tsx contains `animate-card-reveal` class on task card wrappers
    - honeycomb.tsx contains `animationDelay` inline style with `index * 50`
    - honeycomb.tsx empty state contains "Your Honeycomb is empty!" heading
    - honeycomb.tsx empty state contains "The hive is quiet — enjoy it!"
    - honeycomb.tsx heading is `text-xl font-bold`
    - all-tasks.tsx empty state contains "Time to put your Bees to work!"
    - all-tasks.tsx heading is `text-xl font-bold`
    - leaderboard.tsx rank-1 row has `border-l-4 border-amber-400`
    - leaderboard.tsx row hover uses `hover:bg-amber-50` (not stone-100)
    - leaderboard.tsx heading uses `font-bold` (not font-semibold)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>All components reflect bee theme: amber status badges with emoji prefixes, completion flash animation, staggered card reveals, rank-1 amber border, bee-pun empty states, consistent heading typography.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of full bee theme</name>
  <files>none — visual verification only</files>
  <action>
Human visually verifies the complete bee theme. No code changes in this task.

What was built: Complete bee theme applied across the app — amber header with tagline, honeycomb wallpaper, personalized greeting engine, themed status badges, completion flash, card animations, leaderboard rank-1 emphasis, bee-pun copy in empty states.

Steps to verify:
1. Run `make dev` and open http://localhost:3000
2. Log in and navigate to a Hive dashboard
3. Header: Verify amber background with bee emoji, "Honey Do" in dark amber, tagline visible on desktop (hidden on 375px)
4. Greeting: Verify personalized greeting at top of dashboard with your name and task count. Refresh a few times to see variant randomization.
5. Honeycomb pattern: Verify subtle hexagonal pattern visible behind page content
6. Status badges: Create a task, start it — verify "In Progress" badge in amber with bee emoji. Mark done — verify "Done" badge in amber and a brief amber flash on the card.
7. Card animation: Refresh the page — task cards should fade in with slight stagger
8. Leaderboard: Verify rank-1 row has amber left border. Hover over rows — should highlight amber.
9. Mobile: Resize to 375px width. Verify: tagline hidden, greeting text smaller, buttons are 44px tall touch targets, honeycomb pattern is subtle/hidden.
10. Empty states: If possible, test with 0 tasks — verify "Your Honeycomb is empty!" with bee copy.
  </action>
  <verify>Human approval — type "approved" or describe issues</verify>
  <done>Human confirms the full bee theme looks correct across header, greeting, badges, animations, leaderboard, empty states, and mobile viewport.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes (no type errors)
- `grep -c "Hey {name}" src/components/hive/dashboard-greeting.tsx` returns 32 (8 variants x 4 states)
- `grep "DashboardGreeting" src/components/hive/hive-dashboard.tsx` shows import and render
- `grep "bg-amber-100 text-amber-800" src/components/tasks/task-card.tsx` shows themed In Progress badge
- `grep "border-l-4 border-amber-400" src/components/hive/leaderboard.tsx` shows rank-1 emphasis
- `grep "HoneycombPattern" src/app/layout.tsx` shows pattern wrapper
- Visual verification confirms bee theme presence across all pages
</verification>

<success_criteria>
- DashboardGreeting renders with 32 copy variants (8 per task-load state), personalized with user name
- All status badges use amber palette with emoji prefixes
- Task completion triggers 600ms amber flash
- Honeycomb section cards fade in with 50ms stagger
- Leaderboard rank-1 has amber left border, rows hover amber
- All empty states use bee-themed copy per UI-SPEC Copywriting Contract
- All section headings use text-xl font-bold per Typography spec
- Honeycomb wallpaper visible on page background via layout wrapper
- Human approves visual result
</success_criteria>

<output>
After completion, create `.planning/phases/05-theme-copy/05-02-SUMMARY.md`
</output>
